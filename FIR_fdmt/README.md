 This is a verilog project of basic fir. 
 You can use the script  `./Gen/tap_coeff.ipynb` to generate suitable tap coefficients and modify the values in `./src/FIR.v` accordingly.
## Toolchain

iverilog+gtkwave
